# Force LF for all text files, regardless of a contributor's local
# core.autocrlf setting or OS. Without this, a file committed with CRLF
# (common on Windows) diffs on every line when CI (Ubuntu) regenerates it
# with LF, making the build-check workflow spuriously fail.
* text=auto eol=lf
