# This repo tracks only the HTML handbook files.

# Large media (411 MB; exceeds GitHub limits)
audio/

# Vercel deployment metadata
.vercel

# Build scripts, docs (non-content)
# NOTE: _build_index.py and _handbook_template.html are NOT listed here (and
# ARE tracked) — CI (.github/workflows/build-check.yml) needs them present to
# rebuild handbook.html and verify it's not stale.
_build_audiobook.py
HOW-TO-MAKE-AUDIOBOOK.md
_local_vercel_sim.py
_f15/

# Deployment config
# NOTE: vercel.json IS tracked — the GitHub-connected Vercel deploy needs it
# for the "/" -> handbook.html rewrite.
.vercelignore

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
