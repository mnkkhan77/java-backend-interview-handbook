# This repo tracks only the HTML handbook files.

# Large media (411 MB; exceeds GitHub limits)
audio/

# Vercel deployment metadata
.vercel

# Build scripts, templates, and docs (non-content)
_build_audiobook.py
_build_index.py
HOW-TO-MAKE-AUDIOBOOK.md
_handbook_template.html
_index_template.html
_local_vercel_sim.py
_f15/

# Deployment config
# NOTE: vercel.json IS tracked — the GitHub-connected Vercel deploy needs it
# for the "/" -> handbook.html and "/react" -> react/handbook.html rewrites.
.vercelignore

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
